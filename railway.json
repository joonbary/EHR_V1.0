{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "python railway_fix_all_issues.py && python smart_migrate.py && python manage.py collectstatic --noinput && gunicorn ehr_system.wsgi:application --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  },
  "envVars": {
    "DJANGO_SETTINGS_MODULE": "ehr_system.settings",
    "PYTHON_VERSION": "3.11",
    "PORT": "8000"
  }
}
